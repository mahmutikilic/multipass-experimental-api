# Features

- FastAPI application with SQLAlchemy ORM models
- CRUD endpoints for user management under `/v1/users`
- Static HTML interface served from `/ui`
- Health check available at `/status`
- Optional Redis and PostgreSQL services via `docker-compose.yml`
